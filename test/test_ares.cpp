#include <stdio.h>
#include <string.h>
#include <ares.h>
#include <string>

using namespace std;

#define DNS_SERVER_ADDR "8.8.8.8"
#define DNS_SERVER_PORT 53

class AsyncDNSResolver {

private:
    int createQuery(const char* host, unsigned char* buf, size_t len) {

    }

private:
    

};
